# Vite 构建版本使用指南

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

Vite 开发服务器会自动启动在 `http://localhost:8000`

### 3. 构建生产版本

```bash
npm run build
```

构建文件会输出到 `dist/` 目录

### 4. 预览生产版本

```bash
npm run preview
```

## 📁 项目结构

```
.
├── index.vite.html        # Vite 版本的 HTML 入口
├── vite.config.js        # Vite 配置文件
├── package.json          # 项目配置和依赖
├── src/
│   ├── main.jsx          # Vite 入口文件
│   ├── components/
│   │   ├── Icon.js       # 图标组件
│   │   ├── Header.js     # 头部组件
│   │   ├── ExchangeRateCards.js  # 汇率卡片组件
│   │   ├── Sidebar.js    # 侧边栏组件
│   │   ├── ResultsPanel.js # 结果面板组件
│   │   ├── App.js        # 原始 App 组件（CDN 版本）
│   │   └── App.new.js    # 新的 App 组件（模块化版本）
│   ├── utils/
│   │   └── calculations.js # 计算工具函数
│   ├── config/
│   │   └── constants.js  # 常量配置
│   └── styles/
│       └── main.css      # 样式文件
└── dist/                 # 构建输出目录
```

## 🔄 两种版本对比

### CDN 版本（index.html）
- ✅ 无需构建，直接运行
- ✅ 适合快速开发和演示
- ✅ 使用 Babel Standalone 转换 JSX
- ❌ 性能较低
- ❌ 不支持 ES6 模块

### Vite 版本（index.vite.html）
- ✅ 使用 npm 管理依赖
- ✅ 支持 ES6 模块和 JSX
- ✅ 热模块替换（HMR）
- ✅ 生产构建优化
- ✅ TypeScript 支持（可选）
- ❌ 需要安装依赖

## 📝 开发建议

### 使用 Vite 版本进行开发

1. **安装依赖**
   ```bash
   npm install
   ```

2. **启动开发服务器**
   ```bash
   npm run dev
   ```

3. **修改代码**
   - 修改 `src/components/` 中的组件
   - 修改 `src/utils/` 中的工具函数
   - Vite 会自动热更新

4. **构建生产版本**
   ```bash
   npm run build
   ```

### 使用 CDN 版本进行快速测试

1. **启动 Python 服务器**
   ```bash
   npm run dev:python
   ```

2. **访问**
   - 打开 `http://localhost:8000/index.html`

## 🛠️ 下一步优化

- [ ] 完成 Sidebar 组件的完整实现
- [ ] 创建 CostBreakdown 组件
- [ ] 创建 FinancePanel 组件
- [ ] 添加 TypeScript 支持
- [ ] 添加单元测试
- [ ] 添加 ESLint 和 Prettier

## 📚 相关文档

- [Vite 官方文档](https://vitejs.dev/)
- [React 官方文档](https://react.dev/)
- [项目结构说明](./PROJECT_STRUCTURE.md)
