# 列表功能版本说明

## 📋 列表功能位置

### ✅ CDN 模式（当前主要使用）

**列表功能存在于：**
- ✅ `index.modular.html` - CDN 模式入口
- ✅ `src/components/OverseaSection.js` - 海外杂费列表组件
- ✅ `src/components/DomesticSection.js` - 国内杂费列表组件
- ✅ `src/components/Sidebar.js` - 侧边栏组件（包含上述组件）

**访问方式：**
```bash
python3 dev-server.py
# 访问: http://localhost:8000/index.modular.html
```

**组件加载顺序：**
```
index.modular.html
    ↓
加载 OverseaSection.js (包含海外杂费列表)
    ↓
加载 DomesticSection.js (包含国内杂费列表)
    ↓
加载 Sidebar.js (整合所有子组件)
    ↓
加载 App.new.js (主应用组件)
```

---

### ⚠️ Vite 模式（TypeScript 版本）

**当前状态：**
- ⚠️ `App.tsx` 不直接使用 `OverseaSection` 和 `DomesticSection`
- ⚠️ `App.tsx` 直接使用 `Sidebar` 组件
- ⚠️ `Sidebar.js` 是 JavaScript 文件，在 Vite 模式下通过类型声明文件导入

**访问方式：**
```bash
npm run dev
# 访问: http://localhost:8000
```

**组件结构：**
```
index.vite.html
    ↓
src/main.tsx
    ↓
src/components/App.tsx
    ↓
src/components/Sidebar.js (通过类型声明导入)
    ↓
src/components/OverseaSection.js (包含海外杂费列表)
src/components/DomesticSection.js (包含国内杂费列表)
```

---

## 🎯 列表功能详情

### 海外杂费列表

**文件：** `src/components/OverseaSection.js`

**功能：**
- ✅ 添加按钮（列表顶部）
- ✅ 费用项目列表
  - 费用名称输入
  - 费用金额输入
  - 单位切换（RUB/t ↔ RUB/柜）
  - 删除按钮

**使用位置：**
- ✅ CDN 模式：通过 `Sidebar.js` → `OverseaSection.js`
- ⚠️ Vite 模式：通过 `Sidebar.js` → `OverseaSection.js`（JS 文件）

### 国内杂费列表

**文件：** `src/components/DomesticSection.js`

**功能：**
- ✅ 添加按钮（列表顶部）
- ✅ 费用项目列表
  - 费用名称输入
  - 费用金额输入
  - 单位切换（CNY/吨 ↔ CNY/柜）
  - 删除按钮

**使用位置：**
- ✅ CDN 模式：通过 `Sidebar.js` → `DomesticSection.js`
- ⚠️ Vite 模式：通过 `Sidebar.js` → `DomesticSection.js`（JS 文件）

---

## 📊 版本对比

| 功能 | CDN 模式 | Vite 模式 |
|------|----------|-----------|
| **海外杂费列表** | ✅ 可用 | ✅ 可用（通过 JS 文件） |
| **国内杂费列表** | ✅ 可用 | ✅ 可用（通过 JS 文件） |
| **添加按钮位置** | ✅ 列表顶部 | ✅ 列表顶部 |
| **列表样式** | ✅ 统一样式 | ✅ 统一样式 |
| **组件文件** | ✅ `.js` 文件 | ✅ `.js` 文件 |

---

## 🔍 组件调用链

### CDN 模式调用链

```
App.new.js
    ↓
Sidebar.js
    ↓
├── OverseaSection.js ← 海外杂费列表
├── PolicySection.js
└── DomesticSection.js ← 国内杂费列表
```

### Vite 模式调用链

```
App.tsx
    ↓
Sidebar.js (通过类型声明导入)
    ↓
├── OverseaSection.js ← 海外杂费列表
├── PolicySection.js
└── DomesticSection.js ← 国内杂费列表
```

---

## ✅ 总结

**列表功能在两个版本上都存在：**

1. **CDN 模式** (`index.modular.html`)
   - ✅ 完全支持
   - ✅ 使用 JavaScript 文件
   - ✅ 通过全局变量访问

2. **Vite 模式** (`index.vite.html`)
   - ✅ 完全支持
   - ✅ 使用相同的 JavaScript 文件
   - ✅ 通过类型声明文件导入

**列表功能特点：**
- ✅ 添加按钮在列表顶部
- ✅ 统一的视觉样式
- ✅ 完整的 CRUD 功能（添加、编辑、删除、单位切换）

---

**最后更新**: 2026年2月14日
