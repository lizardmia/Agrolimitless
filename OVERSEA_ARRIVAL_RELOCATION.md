# 海外到站预估位置调整

## 📋 修改说明

将"海外到站预估"卡片从 `ExchangeRateCards` 组件移动到 `OverseaSection` 组件下方，使其紧跟在"海外段计算参数"之后。

---

## ✅ 修改内容

### 1. **ExchangeRateCards.js**
- ❌ **移除**："海外到站预估"卡片
- ✅ **调整**：布局从 `grid-cols-3` 改为 `grid-cols-2`（只显示两个汇率卡片）
- ❌ **移除**：`russianArrivalPriceRub` 和 `russianArrivalPriceCny` 参数

### 2. **OverseaSection.js**
- ✅ **添加**：`russianArrivalPriceRub` 和 `russianArrivalPriceCny` 参数
- ✅ **添加**：`formatCurrency` 工具函数引用
- ✅ **添加**："海外到站预估"卡片（位于组件末尾，在海外杂费列表之后）

### 3. **Sidebar.js**
- ✅ **添加**：`russianArrivalPriceRub` 和 `russianArrivalPriceCny` 参数
- ✅ **传递**：将这两个值传递给 `OverseaSection` 组件

### 4. **App.tsx** (Vite/TypeScript 版本)
- ❌ **移除**：从 `ExchangeRateCards` 的 props 中移除 `russianArrivalPriceRub` 和 `russianArrivalPriceCny`
- ✅ **添加**：将这两个值传递给 `Sidebar` 组件

### 5. **App.new.js** (CDN 版本)
- ❌ **移除**：从 `ExchangeRateCards` 的 props 中移除 `russianArrivalPriceRub` 和 `russianArrivalPriceCny`
- ✅ **添加**：将这两个值传递给 `Sidebar` 组件

### 6. **类型声明文件**
- ✅ **更新**：`components.d.ts` - 更新 `ExchangeRateCardsProps` 和 `SidebarProps`
- ✅ **创建**：`OverseaSection.d.ts` - 新增 `OverseaSectionProps` 类型定义

---

## 🎨 视觉效果

### 修改前
```
┌─────────────────────────────────────┐
│  ExchangeRateCards (3列)            │
│  ┌──────┐ ┌──────┐ ┌────────────┐  │
│  │汇率1 │ │汇率2 │ │海外到站预估│  │
│  └──────┘ └──────┘ └────────────┘  │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  OverseaSection                     │
│  1. 海外段计算参数                   │
│  - 农场采购价                        │
│  - 短驳费1/2                         │
│  - 海外杂费列表                      │
└─────────────────────────────────────┘
```

### 修改后
```
┌─────────────────────────────────────┐
│  ExchangeRateCards (2列)            │
│  ┌──────┐ ┌──────┐                  │
│  │汇率1 │ │汇率2 │                  │
│  └──────┘ └──────┘                  │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  OverseaSection                     │
│  1. 海外段计算参数                   │
│  - 农场采购价                        │
│  - 短驳费1/2                         │
│  - 海外杂费列表                      │
│  ┌──────────────────────────────┐   │
│  │ 📍 海外到站预估               │   │
│  │    37,846 RUB/t              │   │
│  │    ≈ ¥ 3,329.23              │   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## 📝 样式说明

"海外到站预估"卡片在 `OverseaSection` 中的样式：
- **背景**：白色 (`bg-white`)
- **边框**：橙色边框 (`border-orange-200`)
- **图标**：地图定位图标 (`MapPin`)，紫色背景 (`bg-indigo-50`)
- **文字**：
  - 标题：小号灰色 (`text-[10px] text-slate-400`)
  - RUB 价格：大号橙色粗体 (`text-xl font-bold text-orange-700`)
  - CNY 价格：小号紫色粗体 (`text-xs font-bold text-indigo-500`)

---

## 🔄 数据流

```
App.tsx
  ├─ results.russianArrivalPriceRub
  └─ results.russianArrivalPriceCny
      │
      └─> Sidebar.js
          │
          └─> OverseaSection.js
              │
              └─> 显示"海外到站预估"卡片
```

---

## ✅ 测试检查

刷新浏览器后，确认：

1. ✅ **ExchangeRateCards** 只显示两个汇率卡片（2列布局）
2. ✅ **OverseaSection** 在"海外段计算参数"下方显示"海外到站预估"卡片
3. ✅ "海外到站预估"显示正确的 RUB 和 CNY 价格
4. ✅ 修改参数后，"海外到站预估"实时更新

---

## 📊 修改文件清单

- ✅ `src/components/ExchangeRateCards.js`
- ✅ `src/components/OverseaSection.js`
- ✅ `src/components/Sidebar.js`
- ✅ `src/components/App.tsx`
- ✅ `src/components/App.new.js`
- ✅ `src/components/components.d.ts`
- ✅ `src/components/OverseaSection.d.ts` (新建)

---

**修改完成时间**: 2026年2月14日
