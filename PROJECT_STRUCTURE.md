# 项目结构说明

## 📁 目录结构

```
.
├── index.html                 # 主 HTML 文件（新版本）
├── pricing-dashboard.html     # 原始单文件版本（保留）
├── package.json               # 项目配置
├── .gitignore                # Git 忽略文件
├── dev-server.py             # Python 开发服务器
├── dev-server.sh             # Shell 脚本开发服务器
│
├── src/                      # 源代码目录
│   ├── index.js              # 入口文件
│   │
│   ├── components/           # 组件目录
│   │   ├── Icon.js           # 图标组件
│   │   └── App.js            # 主应用组件
│   │
│   ├── styles/               # 样式目录
│   │   └── main.css          # 主样式文件
│   │
│   └── utils/                # 工具函数目录（可选）
│       └── calculations.js   # 计算工具函数（待创建）
│
└── docs/                     # 文档目录
    ├── README.md             # 开发调试指南
    ├── 使用说明.md           # 功能使用说明
    ├── 计算规则文档.md       # 计算规则文档
    ├── Cursor调试指南.md     # Cursor 调试指南
    └── 快速开始.md           # 快速开始指南
```

## 📝 文件说明

### 核心文件

- **index.html**: 新的主 HTML 文件，使用模块化结构
- **pricing-dashboard.html**: 原始单文件版本（保留作为备份）

### 源代码文件

- **src/index.js**: 应用入口文件，负责初始化 React 应用
- **src/components/Icon.js**: 图标组件，包含所有 SVG 图标
- **src/components/App.js**: 主应用组件，包含所有业务逻辑和 UI
- **src/styles/main.css**: 主样式文件，包含动画和自定义样式

### 配置文件

- **package.json**: 项目配置文件，包含项目信息和脚本
- **.gitignore**: Git 忽略文件配置

### 开发服务器

- **dev-server.py**: Python 开发服务器（推荐）
- **dev-server.sh**: Shell 脚本开发服务器

## 🚀 使用方法

### 启动开发服务器

```bash
# 方法一：使用 Python 脚本（推荐）
python3 dev-server.py

# 方法二：使用 Shell 脚本
./dev-server.sh

# 方法三：使用 Python 内置服务器
python3 -m http.server 8000
```

### 访问应用

服务器启动后，访问：
- **新版本**: `http://localhost:8000/index.html`
- **旧版本**: `http://localhost:8000/pricing-dashboard.html`（如果保留）

## 🔄 迁移说明

### 从单文件版本迁移

1. **保留原文件**: `pricing-dashboard.html` 已保留，可以继续使用
2. **使用新结构**: 新版本使用模块化结构，便于维护和扩展
3. **逐步迁移**: 可以逐步将功能拆分到独立文件

### 文件对应关系

| 原文件位置 | 新文件位置 | 说明 |
|-----------|-----------|------|
| `<style>` 标签 | `src/styles/main.css` | 样式提取 |
| `const Icon` | `src/components/Icon.js` | 图标组件 |
| `const App` | `src/components/App.js` | 主应用组件 |
| `renderApp()` | `src/index.js` | 入口逻辑 |

## 📦 依赖说明

### CDN 依赖

项目使用 CDN 加载以下库：
- **Tailwind CSS**: `https://cdn.tailwindcss.com`
- **React 18**: `https://unpkg.com/react@18/umd/react.development.js`
- **React DOM 18**: `https://unpkg.com/react-dom@18/umd/react-dom.development.js`
- **Babel Standalone**: `https://unpkg.com/@babel/standalone/babel.min.js`

### 本地依赖

无需安装任何 npm 包，所有依赖通过 CDN 加载。

## 🛠️ 开发建议

### 代码组织

1. **组件拆分**: 可以将 App.js 进一步拆分为更小的组件
2. **工具函数**: 将计算逻辑提取到 `src/utils/calculations.js`
3. **常量配置**: 创建 `src/config/constants.js` 存储配置常量

### 未来扩展

- [ ] 添加构建工具（Webpack/Vite）
- [ ] 使用 npm 包管理依赖
- [ ] 添加 TypeScript 支持
- [ ] 添加单元测试
- [ ] 添加 ESLint 和 Prettier

## 📚 相关文档

- [README.md](./README.md) - 开发调试指南
- [使用说明.md](./使用说明.md) - 功能使用说明
- [计算规则文档.md](./计算规则文档.md) - 计算规则文档
- [Cursor调试指南.md](./Cursor调试指南.md) - Cursor 调试指南

---

**版本**: 1.0  
**最后更新**: 2026年2月
